version: '3.8'

services:
  postgres:
    image: postgres:16.4
    container_name: pagos360-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-pagosuser}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-pagospass}
      POSTGRES_DB:   ${POSTGRES_DB:-pagos360}
    ports:
      - "${PG_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  adminer:
    image: adminer
    container_name: pagos360-adminer
    restart: unless-stopped
    ports:
      - "${ADMINER_PORT:-8080}:8080"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pgdata:
